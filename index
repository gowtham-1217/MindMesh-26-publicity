<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MINDMESH'26 — Publicity Dashboard</title>
<style>
  :root{
    --ink:#141326;
    --ink-soft:#4b4964;
    --paper:#f6f5fb;
    --card:#ffffff;
    --violet:#5b2eea;
    --violet-deep:#37146e;
    --mesh:#1b1440;
    --amber:#ffb020;
    --green:#0e9f6e;
    --red:#e02424;
    --line:#e6e3f2;
    --radius:14px;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  body{
    font-family:'Segoe UI',system-ui,-apple-system,Roboto,sans-serif;
    background:var(--paper);color:var(--ink);
    -webkit-font-smoothing:antialiased;
  }
  /* ---------- HEADER ---------- */
  header{
    background:
      radial-gradient(circle at 85% 20%, rgba(91,46,234,.5), transparent 45%),
      radial-gradient(circle at 10% 90%, rgba(255,176,32,.25), transparent 40%),
      var(--mesh);
    color:#fff;
    padding:28px 20px 88px;
    position:relative;
    overflow:hidden;
  }
  header svg.mesh{position:absolute;inset:0;width:100%;height:100%;opacity:.25;pointer-events:none}
  .head-inner{max-width:1080px;margin:0 auto;position:relative}
  .eyebrow{font-size:12px;letter-spacing:.22em;text-transform:uppercase;color:#c9bcf6;font-weight:600}
  .dept{font-size:13px;color:#a99ce0;margin-top:4px}
  h1{font-size:clamp(26px,5vw,40px);font-weight:800;margin-top:12px;letter-spacing:-.01em}
  h1 .yr{color:var(--amber)}
  .sub{color:#bdb3e6;font-size:14px;margin-top:6px}

  /* ---------- STATS ---------- */
  .stats{
    max-width:1080px;margin:-64px auto 0;padding:0 20px;
    display:grid;grid-template-columns:repeat(3,1fr);gap:14px;position:relative;
  }
  .stat{
    background:var(--card);border:1px solid var(--line);border-radius:var(--radius);
    padding:16px 18px;box-shadow:0 6px 20px rgba(27,20,64,.08);
  }
  .stat .ico{font-size:20px}
  .stat .num{font-size:28px;font-weight:800;margin-top:4px;font-variant-numeric:tabular-nums}
  .stat .lbl{font-size:12px;color:var(--ink-soft);margin-top:2px}
  .stat.target .num{color:var(--violet)}

  /* target progress */
  .progress-wrap{max-width:1080px;margin:14px auto 0;padding:0 20px}
  .progress-card{
    background:var(--card);border:1px solid var(--line);border-radius:var(--radius);
    padding:14px 18px;
  }
  .progress-top{display:flex;justify-content:space-between;font-size:13px;color:var(--ink-soft);margin-bottom:8px}
  .bar{height:10px;background:var(--line);border-radius:99px;overflow:hidden}
  .bar>i{display:block;height:100%;width:0%;border-radius:99px;
    background:linear-gradient(90deg,var(--violet),var(--amber));transition:width .6s ease}

  /* ---------- LAYOUT ---------- */
  main{max-width:1080px;margin:22px auto 60px;padding:0 20px;display:grid;gap:18px}
  .card{
    background:var(--card);border:1px solid var(--line);border-radius:var(--radius);
    padding:20px;
  }
  .card h2{font-size:16px;font-weight:700;margin-bottom:14px;display:flex;align-items:center;gap:8px}

  /* form */
  .grid2{display:grid;grid-template-columns:1fr 1fr;gap:12px}
  label{font-size:12px;font-weight:600;color:var(--ink-soft);display:block;margin-bottom:4px}
  input[type=text],input[type=tel],input[type=number],select{
    width:100%;padding:10px 12px;border:1px solid var(--line);border-radius:10px;
    font-size:14px;background:#fbfaff;color:var(--ink);outline:none;
  }
  input:focus,select:focus{border-color:var(--violet);box-shadow:0 0 0 3px rgba(91,46,234,.12)}
  .field{margin-bottom:2px}
  .btn{
    border:none;border-radius:10px;padding:11px 18px;font-size:14px;font-weight:700;
    cursor:pointer;transition:.15s;
  }
  .btn-primary{background:var(--violet);color:#fff}
  .btn-primary:hover{background:var(--violet-deep)}
  .btn-ghost{background:transparent;color:var(--ink-soft);border:1px solid var(--line)}
  .btn-ghost:hover{border-color:var(--ink-soft)}
  .form-actions{display:flex;gap:10px;margin-top:14px;flex-wrap:wrap}
  .edit-note{display:none;font-size:13px;background:#fff8e8;border:1px solid #f3d68b;color:#8a6100;
    padding:8px 12px;border-radius:10px;margin-bottom:12px}

  /* toolbar */
  .toolbar{display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-bottom:14px}
  .search{flex:1;min-width:200px;position:relative}
  .search input{padding-left:34px}
  .search .mag{position:absolute;left:11px;top:50%;transform:translateY(-50%);font-size:14px;opacity:.5}

  /* table */
  .tbl-wrap{overflow-x:auto}
  table{width:100%;border-collapse:collapse;font-size:14px}
  th{font-size:11px;text-transform:uppercase;letter-spacing:.08em;color:var(--ink-soft);
    text-align:left;padding:10px 12px;border-bottom:2px solid var(--line)}
  td{padding:12px;border-bottom:1px solid var(--line);vertical-align:top}
  tr:hover td{background:#faf9ff}
  .cname{font-weight:600}
  .csub{font-size:12px;color:var(--ink-soft);margin-top:2px}
  .badge{display:inline-block;font-size:12px;font-weight:700;padding:3px 10px;border-radius:99px}
  .badge.Confirmed{background:#e6f7f1;color:var(--green)}
  .badge.Pending{background:#fff4e0;color:#b76e00}
  .row-actions button{background:none;border:none;cursor:pointer;font-size:16px;padding:4px 6px;border-radius:6px}
  .row-actions button:hover{background:var(--line)}
  .empty{padding:34px 12px;text-align:center;color:var(--ink-soft);font-size:14px}

  /* chart + ranking */
  .two-col{display:grid;grid-template-columns:1.4fr 1fr;gap:18px}
  .hbar{margin-bottom:12px}
  .hbar .hb-top{display:flex;justify-content:space-between;font-size:13px;margin-bottom:4px}
  .hbar .hb-top b{font-variant-numeric:tabular-nums}
  .hbar .track{height:12px;background:var(--line);border-radius:99px;overflow:hidden}
  .hbar .fill{height:100%;border-radius:99px;background:var(--violet);transition:width .5s ease}
  .rank-item{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}
  .rank-item:last-child{border-bottom:none}
  .medal{width:34px;height:34px;border-radius:50%;display:flex;align-items:center;justify-content:center;
    font-weight:800;font-size:14px;background:var(--line);color:var(--ink-soft);flex-shrink:0}
  .medal.m1{background:#ffe9b8;color:#8a6100}
  .medal.m2{background:#e8e8ef;color:#555}
  .medal.m3{background:#f4dcc8;color:#8a4b12}
  .rank-name{font-weight:600;font-size:14px}
  .rank-count{margin-left:auto;font-weight:700;color:var(--violet);font-variant-numeric:tabular-nums}

  .toast{
    position:fixed;bottom:22px;left:50%;transform:translateX(-50%) translateY(80px);
    background:var(--ink);color:#fff;padding:11px 20px;border-radius:99px;font-size:14px;
    opacity:0;transition:.3s;z-index:50;box-shadow:0 10px 30px rgba(0,0,0,.25)
  }
  .toast.show{opacity:1;transform:translateX(-50%) translateY(0)}

  footer{max-width:1080px;margin:0 auto 30px;padding:0 20px;font-size:12px;color:var(--ink-soft);text-align:center}

  /* ---------- MOBILE ---------- */
  @media (max-width:760px){
    .stats{grid-template-columns:repeat(2,1fr)}
    .grid2,.two-col{grid-template-columns:1fr}
    /* table -> cards */
    table thead{display:none}
    table,tbody,tr,td{display:block;width:100%}
    tr{border:1px solid var(--line);border-radius:12px;margin-bottom:12px;padding:6px 4px;background:#fff}
    td{border:none;padding:6px 12px}
    td[data-l]::before{content:attr(data-l);display:block;font-size:10px;text-transform:uppercase;
      letter-spacing:.08em;color:var(--ink-soft);margin-bottom:2px}
    tr:hover td{background:transparent}
  }
  @media (prefers-reduced-motion:reduce){*{transition:none!important}}
</style>
</head>
<body>

<header>
  <svg class="mesh" viewBox="0 0 800 300" preserveAspectRatio="xMidYMid slice" aria-hidden="true">
    <g stroke="#8f7bff" stroke-width="1" fill="none">
      <path d="M40 60 L180 30 L320 90 L470 40 L620 100 L760 50"/>
      <path d="M40 60 L120 180 L320 90 M180 30 L120 180 M320 90 L400 220 L470 40 M400 220 L620 100 L560 250 L760 50 M560 250 L400 220 M120 180 L280 260 L400 220 M280 260 L560 250"/>
    </g>
    <g fill="#c9bcf6">
      <circle cx="40" cy="60" r="4"/><circle cx="180" cy="30" r="4"/><circle cx="320" cy="90" r="5"/>
      <circle cx="470" cy="40" r="4"/><circle cx="620" cy="100" r="5"/><circle cx="760" cy="50" r="4"/>
      <circle cx="120" cy="180" r="4"/><circle cx="400" cy="220" r="5"/><circle cx="280" cy="260" r="4"/>
      <circle cx="560" cy="250" r="4"/>
    </g>
  </svg>
  <div class="head-inner">
    <div class="eyebrow">Sacred Heart College (Autonomous), Tirupattur</div>
    <div class="dept">Department of Artificial Intelligence &amp; Machine Learning</div>
    <h1>MINDMESH<span class="yr">'26</span> · Publicity Dashboard</h1>
    <div class="sub">Track college outreach and student counts — live.</div>
  </div>
</header>

<!-- STATS -->
<section class="stats">
  <div class="stat"><div class="ico">🏫</div><div class="num" id="stColleges">0</div><div class="lbl">Total Colleges</div></div>
  <div class="stat"><div class="ico">👨‍🎓</div><div class="num" id="stStudents">0</div><div class="lbl">Total Students</div></div>
  <div class="stat target"><div class="ico">📈</div><div class="num">500+</div><div class="lbl">Target Students</div></div>
</section>

<div class="progress-wrap">
  <div class="progress-card">
    <div class="progress-top"><span>Progress toward target</span><span id="pgLabel">0 / 500</span></div>
    <div class="bar"><i id="pgFill"></i></div>
  </div>
</div>

<main>

  <!-- FORM -->
  <section class="card" id="formCard">
    <h2>➕ College Entry</h2>
    <div class="edit-note" id="editNote">✏️ Editing an existing college — save changes or cancel.</div>
    <div class="grid2">
      <div class="field">
        <label for="fName">College Name</label>
        <input type="text" id="fName" placeholder="e.g. Priyadarshini Engineering College">
      </div>
      <div class="field">
        <label for="fCount">Student Count</label>
        <input type="number" id="fCount" min="0" placeholder="e.g. 45">
      </div>
      <div class="field">
        <label for="fPerson">Contact Person</label>
        <input type="text" id="fPerson" placeholder="Coordinator name">
      </div>
      <div class="field">
        <label for="fPhone">Contact Number</label>
        <input type="tel" id="fPhone" placeholder="10-digit mobile">
      </div>
    </div>
    <div class="grid2" style="margin-top:12px">
      <div class="field">
        <label for="fStatus">Status</label>
        <select id="fStatus">
          <option>Pending</option>
          <option>Confirmed</option>
        </select>
      </div>
    </div>
    <div class="form-actions">
      <button class="btn btn-primary" id="btnSave" onclick="saveCollege()">➕ Add College</button>
      <button class="btn btn-ghost" id="btnCancel" style="display:none" onclick="cancelEdit()">Cancel</button>
    </div>
  </section>

  <!-- TABLE -->
  <section class="card">
    <h2>📋 College-wise Table</h2>
    <div class="toolbar">
      <div class="search">
        <span class="mag">🔍</span>
        <input type="text" id="searchBox" placeholder="Search college or contact…" oninput="render()">
      </div>
      <button class="btn btn-ghost" onclick="exportCSV()">📥 Export CSV</button>
    </div>
    <div class="tbl-wrap">
      <table>
        <thead>
          <tr><th>#</th><th>College Name</th><th>Students</th><th>Status</th><th></th></tr>
        </thead>
        <tbody id="tbody"></tbody>
      </table>
      <div class="empty" id="emptyMsg" style="display:none">No colleges yet — add your first one above. 🚀</div>
    </div>
  </section>

  <!-- CHART + RANKING -->
  <div class="two-col">
    <section class="card">
      <h2>📊 Students per College</h2>
      <div id="chart"></div>
      <div class="empty" id="chartEmpty" style="display:none">Chart appears once colleges are added.</div>
    </section>
    <section class="card">
      <h2>🏆 Top Participating Colleges</h2>
      <div id="ranking"></div>
      <div class="empty" id="rankEmpty" style="display:none">Ranking appears once colleges are added.</div>
    </section>
  </div>

</main>

<footer>MINDMESH'26 · Dept. of AI &amp; ML · Sacred Heart College (Autonomous), Tirupattur</footer>
<div class="toast" id="toast"></div>

<script>
const STORAGE_KEY = 'mindmesh26-colleges';
let colleges = [];
let editId = null;

/* ---------- storage ---------- */
async function load(){
  try{
    const res = await window.storage.get(STORAGE_KEY);
    if(res && res.value) colleges = JSON.parse(res.value);
  }catch(e){ colleges = []; }
  render();
}
async function persist(){
  try{ await window.storage.set(STORAGE_KEY, JSON.stringify(colleges)); }
  catch(e){ toast('⚠️ Could not save — check connection'); }
}

/* ---------- CRUD ---------- */
function saveCollege(){
  const name = document.getElementById('fName').value.trim();
  const count = parseInt(document.getElementById('fCount').value,10);
  const person = document.getElementById('fPerson').value.trim();
  const phone = document.getElementById('fPhone').value.trim();
  const status = document.getElementById('fStatus').value;

  if(!name){ toast('Enter the college name'); return; }
  if(isNaN(count) || count < 0){ toast('Enter a valid student count'); return; }
  if(phone && !/^\d{10}$/.test(phone)){ toast('Contact number should be 10 digits'); return; }

  const entry = { name, count, person, phone, status };

  if(editId !== null){
    const i = colleges.findIndex(c=>c.id===editId);
    if(i>-1) colleges[i] = {...entry, id:editId};
    toast('✅ College updated');
    cancelEdit(false);
  }else{
    entry.id = Date.now();
    colleges.push(entry);
    toast('✅ College added');
  }
  clearForm();
  persist();
  render();
}
function editCollege(id){
  const c = colleges.find(x=>x.id===id); if(!c) return;
  editId = id;
  document.getElementById('fName').value = c.name;
  document.getElementById('fCount').value = c.count;
  document.getElementById('fPerson').value = c.person||'';
  document.getElementById('fPhone').value = c.phone||'';
  document.getElementById('fStatus').value = c.status||'Pending';
  document.getElementById('editNote').style.display='block';
  document.getElementById('btnSave').textContent='💾 Save Changes';
  document.getElementById('btnCancel').style.display='inline-block';
  document.getElementById('formCard').scrollIntoView({behavior:'smooth'});
}
function cancelEdit(clear=true){
  editId = null;
  if(clear) clearForm();
  document.getElementById('editNote').style.display='none';
  document.getElementById('btnSave').textContent='➕ Add College';
  document.getElementById('btnCancel').style.display='none';
}
function deleteCollege(id){
  const c = colleges.find(x=>x.id===id); if(!c) return;
  if(!confirm(`Delete "${c.name}"?`)) return;
  colleges = colleges.filter(x=>x.id!==id);
  if(editId===id) cancelEdit();
  toast('🗑️ College removed');
  persist(); render();
}
function clearForm(){
  ['fName','fCount','fPerson','fPhone'].forEach(i=>document.getElementById(i).value='');
  document.getElementById('fStatus').value='Pending';
}

/* ---------- render ---------- */
function esc(s){ return String(s??'').replace(/[&<>"']/g, m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); }

function render(){
  const q = document.getElementById('searchBox').value.trim().toLowerCase();
  const list = colleges.filter(c=>{
    if(!q) return true;
    return c.name.toLowerCase().includes(q)
      || (c.person||'').toLowerCase().includes(q);
  });

  /* stats */
  document.getElementById('stColleges').textContent = colleges.length;
  const total = colleges.reduce((s,c)=>s+(c.count||0),0);
  document.getElementById('stStudents').textContent = total;
  const pct = Math.min(100, Math.round(total/500*100));
  document.getElementById('pgFill').style.width = pct+'%';
  document.getElementById('pgLabel').textContent = `${total} / 500 (${pct}%)`;

  /* table */
  const tb = document.getElementById('tbody');
  tb.innerHTML = '';
  document.getElementById('emptyMsg').style.display = list.length ? 'none':'block';
  list.forEach((c,i)=>{
    const tr = document.createElement('tr');
    tr.innerHTML = `
      <td data-l="#">${i+1}</td>
      <td data-l="College">
        <div class="cname">${esc(c.name)}</div>
        ${c.person||c.phone ? `<div class="csub">${esc(c.person||'')}${c.person&&c.phone?' · ':''}${c.phone?`<a href="tel:${esc(c.phone)}">${esc(c.phone)}</a>`:''}</div>`:''}
      </td>
      <td data-l="Students"><b>${c.count}</b></td>
      <td data-l="Status"><span class="badge ${c.status}">${esc(c.status)}</span></td>
      <td data-l="Actions" class="row-actions">
        <button title="Edit" onclick="editCollege(${c.id})">✏️</button>
        <button title="Delete" onclick="deleteCollege(${c.id})">🗑️</button>
      </td>`;
    tb.appendChild(tr);
  });

  /* chart */
  const chart = document.getElementById('chart');
  chart.innerHTML='';
  const sorted = [...colleges].sort((a,b)=>b.count-a.count);
  document.getElementById('chartEmpty').style.display = sorted.length ? 'none':'block';
  const max = sorted.length ? sorted[0].count || 1 : 1;
  sorted.forEach(c=>{
    const d = document.createElement('div');
    d.className='hbar';
    d.innerHTML = `<div class="hb-top"><span>${esc(c.name)}</span><b>${c.count}</b></div>
      <div class="track"><div class="fill" style="width:${Math.max(3,(c.count/max)*100)}%"></div></div>`;
    chart.appendChild(d);
  });

  /* ranking */
  const rank = document.getElementById('ranking');
  rank.innerHTML='';
  document.getElementById('rankEmpty').style.display = sorted.length ? 'none':'block';
  sorted.slice(0,5).forEach((c,i)=>{
    const d = document.createElement('div');
    d.className='rank-item';
    d.innerHTML = `<div class="medal m${i+1}">${i+1}</div>
      <div><div class="rank-name">${esc(c.name)}</div>
      <div class="csub">${c.person ? esc(c.person)+' · ' : ''}${esc(c.status)}</div></div>
      <div class="rank-count">${c.count}</div>`;
    rank.appendChild(d);
  });
}

/* ---------- export ---------- */
function exportCSV(){
  if(!colleges.length){ toast('Nothing to export yet'); return; }
  const rows = [['#','College Name','Students','Contact Person','Contact Number','Status']];
  colleges.forEach((c,i)=>rows.push([
    i+1, c.name, c.count, c.person||'', c.phone||'', c.status
  ]));
  const csv = rows.map(r=>r.map(v=>`"${String(v).replace(/"/g,'""')}"`).join(',')).join('\n');
  const blob = new Blob(['\uFEFF'+csv], {type:'text/csv;charset=utf-8'});
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = 'MINDMESH26_publicity.csv';
  a.click();
  URL.revokeObjectURL(a.href);
  toast('📥 CSV downloaded — opens in Excel/Sheets');
}

/* ---------- toast ---------- */
let toastTimer;
function toast(msg){
  const t = document.getElementById('toast');
  t.textContent = msg; t.classList.add('show');
  clearTimeout(toastTimer);
  toastTimer = setTimeout(()=>t.classList.remove('show'), 2400);
}

load();
</script>
</body>
</html>
